ChinaTradeResolve Document AI v3.7.55

Точечное техническое исправление IndexNow без изменения интерфейса и базы данных.

Что исправлено:
- команда `python scripts/submit_indexnow.py` теперь работает напрямую из Render Shell и корня проекта;
- скрипт самостоятельно добавляет корень проекта в путь импорта, поэтому ошибка `ModuleNotFoundError: No module named app` больше не возникает;
- `keyLocation` исправлен с `/indexnow/<ключ>.txt` на корневой `/<ключ>.txt`;
- корневое расположение ключа разрешает отправлять все публичные языковые страницы, поэтому ошибка HTTP 422 устранена;
- список из 84 URL дедуплицируется перед отправкой;
- добавлены автоматические тесты для обоих исправлений.

После установки используйте обычную команду:

    python scripts/submit_indexnow.py

Ожидаемые ответы IndexNow: HTTP 200 или HTTP 202.
