# Подключение ИИ‑помощника на Render

## 1. Создайте отдельный ключ OpenAI

Создайте API‑ключ для проекта ChinaTradeResolve. Не отправляйте его в чат, не вставляйте в HTML и не загружайте в GitHub.

## 2. Добавьте переменные в Render

Откройте ваш Web Service → **Environment** → **Add Environment Variable** и добавьте:

```text
ENABLE_AI_ASSISTANT=true
OPENAI_API_KEY=ваш_секретный_ключ
OPENAI_MODEL=модель_доступная_в_вашем_OpenAI_проекте
OPENAI_MODERATION_MODEL=omni-moderation-latest
AI_ASSISTANT_MAX_OUTPUT_TOKENS=500
AI_ASSISTANT_HISTORY_MESSAGES=8
```

Отдельная модель для чата необязательна:

```text
OPENAI_ASSISTANT_MODEL=
```

Если переменная пустая, используется `OPENAI_MODEL`.

## 3. Выполните развёртывание

Сохраните переменные и запустите **Manual Deploy → Deploy latest commit** или загрузите новую сборку тем же способом, которым вы обновляли сайт раньше.

## 4. Проверьте состояние

Откройте:

```text
https://chinatraderesolve.onrender.com/health
```

Ожидаемый фрагмент ответа:

```json
{
  "ai_assistant_enabled": true
}
```

Если значение `false`, проверьте три обязательных пункта: `ENABLE_AI_ASSISTANT=true`, непустой `OPENAI_API_KEY` и непустая модель.

## 5. Проверьте интерфейс

На главной странице должна появиться кнопка **«Спросить ИИ‑помощника»**. Проверьте:

1. русский, английский, французский, немецкий, испанский и сербский языки;
2. три быстрых вопроса;
3. обычное сообщение;
4. предупреждение о конфиденциальности;
5. работу на телефоне;
6. отсутствие ключа API в исходном HTML страницы.

## 6. Контроль расходов и безопасности

- Не увеличивайте лимиты, пока не увидите реальную нагрузку.
- Следите за расходами и лимитами в кабинете OpenAI.
- При подозрении на утечку немедленно отзовите ключ и создайте новый.
- Не давайте помощнику прямой доступ к базе дел на этом этапе.
- Для доступа к конкретным делам позже понадобится отдельная авторизация, журнал действий и более строгая модель разрешений.

## 7. Что уже защищено в коде

- ключ используется только сервером;
- запрос идёт через `/api/assistant`;
- ответы не сохраняются у приложения;
- в запрос OpenAI передаются только последние сообщения;
- `store: false`;
- ограничение 18 запросов за 10 минут с одного подключения;
- сообщения до 1500 символов в интерфейсе;
- история до 8 последних сообщений;
- ответ ограничен по длине;
- ИИ не должен обещать результат, давать обязательное юридическое заключение или просить секреты.
